# URL base do backend (carrawave-app-api). Em dev local, aponta para o
# Spring Boot rodando na sua máquina; em produção, aponte para o domínio
# publicado (Railway/Render/etc).
VITE_API_URL=http://localhost:8080
